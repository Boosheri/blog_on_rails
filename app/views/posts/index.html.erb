
<div class="cards">
  <% @posts.each do |post| %>
    <div class="card">
    <div class="card-body">
        
        <%= link_to(post.title, post_path(post)) %> <br />
        <small>
            Posted <%= time_ago_in_words(post.created_at)  %> ago
        </small>
    
    </div>
    </div>
  <% end %>
</div>
